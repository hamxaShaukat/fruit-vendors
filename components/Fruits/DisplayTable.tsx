import React from "react";

interface TitleProps {
  name: string;
  dataOfShipment:string;
  totalQuantity:string;
  unitPrice:string;
  totalCost:string;
}
const DisplayTable = ({
    name,
    dataOfShipment,
    totalQuantity,
    unitPrice,
    totalCost,
  }: TitleProps
) => {
  return (
  <>
    
    
  </>
  );
};

export default DisplayTable;
